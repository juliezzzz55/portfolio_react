function SkillBadge({ name }) {
  return (
    <span style={{ background: '#1a1a1a', border: '1px solid #444', borderRadius: '20px', padding: '6px 16px', fontSize: '0.9rem' }}>
      {name}
    </span>
  )
}

export default SkillBadge
